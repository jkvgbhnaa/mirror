# LinkVault 聚合导航系统

LinkVault 是一个面向技术内容创作者、开发者资讯平台及个人知识管理者的高可用外链聚合与导航系统。该项目并非传统意义上的内容管理系统，而是一个轻量级、可自托管的 URL 资产编排层，用于将分散于各类临时性新闻源、技术博客或活动页面的超链接，按照统一的数据结构进行抓取、分类、去重与呈现。系统核心定位为“外链的索引层”，帮助用户在不直接存储大量正文内容的前提下，维护一份高价值、可检索、可追溯的链接清单，并对外提供只读 API 与静态快照页面。LinkVault 适用于需要定期整理月度技术简报、聚合多来源活动通知、或建立团队内部知识外链仓库的场景，尤其擅长处理带有日期路径与无规律 ID 的第三方资讯链接。

## 功能概览

- **原始链接无损入库**：系统对导入的 URL 执行最小化处理，保留协议、域名、路径及查询参数原始形态，不做主动跳转或协议升级，确保来源可溯。
- **批次化资源管理**：支持按批次（如 90 批次）对链接进行逻辑分组，每个批次可记录导入时间、预期总数与当前状态，便于运营人员追踪资源采集进度。
- **自动元信息补全**：对于入库链接，系统会异步执行标题提取与摘要生成（基于 HTTP 响应头部与文档标题），补全链接的语义标签，提升浏览可读性。
- **多端点响应适配**：根据访问来源（移动端 H5、WAP、或 3G 门户）自动切换响应模板，确保在 `h5.yidianmeii.cn`、`wap.yidianmeii.cn` 与 `3g.yidianmeii.cn` 等子域名下均能获得适配的视图样式。
- **去重与冲突检测**：基于 URL 完整字符串的哈希索引，防止同一资源链接在相同批次内或跨批次重复录入，并提供冲突合并选项。
- **只读只存不缓存**：系统不缓存目标页面的完整正文或静态资源，仅存储元数据，降低存储开销，并强制每次访问时重定向至原始地址，确保内容始终为源站最新状态。

## 应用场景

- **月度技术资讯汇总**：技术社区运营人员可将当月发布的数十篇外部报道、版本发布公告、技术演讲录像链接，通过 LinkVault 统一入库，生成一份带时间戳的导航页，供订阅用户一站式浏览。
- **临时活动页面聚合**：在举办线上黑客松或技术沙龙期间，组织者将分散在多个合作媒体上的报名页、直播入口、资料下载地址，快速录入系统，生成专属活动链接集合，并通过单一短链对外分发。
- **团队内部知识引用库**：研发团队在编写设计文档或复盘报告时，经常需要引用外部 issue 跟踪链接、代码仓库提交记录或第三方性能测试报告。LinkVault 可作为这些外部引用的统一管理入口，避免文档中散落大量冗长 URL。
- **内容审核中间件**：内容审核团队在审核用户提交的外部链接时，可将待审链接先行导入 LinkVault 的“待审核批次”，结合手动标注功能，标记可疑域名或违规路径，审核通过后再对外公开。

## 快速开始

以下步骤将在本地环境启动 LinkVault 服务的开发实例，端口默认监听 8080。

```bash
# 克隆项目仓库至本地
git clone https://github.com/linkvault/linkvault-core.git
cd linkvault-core

# 安装项目依赖（使用 npm，确保 Node.js 版本 >= 18.0）
npm install

# 使用示例数据启动服务（包含第 28/90 批次的预置链接）
npm run seed:batch-28
npm start
```

启动成功后，访问 `http://localhost:8080/batch/28` 即可查看当前批次的链接列表视图。若需导入自定义链接，可通过 `POST /api/links` 接口提交 JSON 格式的 URL 数组。

## 安装要求

| 依赖组件          | 必需版本                      | 说明                                                                 |
|-------------------|-------------------------------|----------------------------------------------------------------------|
| Node.js           | 18.x LTS 或更高               | 运行时环境，提供 ES 模块与异步 HTTP 请求支持                         |
| npm               | 8.x 或更高                    | 依赖管理工具，用于安装项目第三方库                                   |
| SQLite3           | 3.35 或更高（内嵌于项目）     | 内置轻量级数据库，用于存储链接元数据与批次信息，无需额外安装         |
| Redis（可选）     | 6.0 或更高                    | 若启用分布式去重锁或 API 限流功能，需额外配置 Redis 实例             |
| 网络环境          | 可访问外网                    | 系统在导入链接时需对目标域名发起 HEAD/GET 请求以提取元信息           |

## 文档导航

| 层面          | 目录                      | 回答的问题                                                               |
|---------------|---------------------------|--------------------------------------------------------------------------|
| 运维部署      | `/docs/deployment.md`     | 如何将系统部署至生产环境？支持哪些容器化方案？环境变量如何配置？        |
| API 参考      | `/docs/api/v1/links.md`   | 链接导入、查询、删除的接口定义是什么？请求与响应的数据结构如何设计？    |
| 批次管理      | `/docs/batch-management.md`| 如何创建新批次？如何向已有批次追加链接？批次状态（开放/关闭/归档）如何流转？ |
| 前端适配      | `/docs/frontend-adapters.md`| 系统如何识别访问来源为 `h5`、`wap` 或 `3g`？模板渲染逻辑能否自定义扩展？ |

## 资源列表

- http://3g.yidianmeii.cn/jnews/0814/383937.shtml
- http://3g.yidianmeii.cn/jnews/0814/980290.shtml
- http://wap.yidianmeii.cn/jnews/0814/0836.shtml
- http://h5.yidianmeii.cn/jnews/0814/5382.shtml
- http://h5.yidianmeii.cn/jnews/0814/722040.shtml
- http://3g.yidianmeii.cn/jnews/0814/7867.shtml
- http://wap.yidianmeii.cn/jnews/0814/971594.shtml
- http://wap.yidianmeii.cn/jnews/0814/03207.shtml
- http://3g.yidianmeii.cn/jnews/0814/0975698.shtml
- http://wap.yidianmeii.cn/jnews/0814/9254.shtml

## 项目结构

```
linkvault-core/
├── src/
│   ├── adapters/                # 前端适配器：根据请求头识别 h5/wap/3g 并选择模板
│   │   ├── h5Renderer.js        # H5 端渲染逻辑
│   │   ├── wapRenderer.js       # WAP 端渲染逻辑
│   │   └── g3Renderer.js        # 3G 端渲染逻辑
│   ├── api/                     # RESTful API 路由定义
│   │   ├── links.js             # 链接增删改查及去重接口
│   │   └── batches.js           # 批次管理接口
│   ├── core/                    # 核心业务逻辑
│   │   ├── importer.js          # 链接导入引擎，含元信息提取与哈希计算
│   │   ├── dedup.js             # 基于 URL 哈希的去重模块
│   │   └── resolver.js          # 原始 URL 解析与重定向生成器
│   ├── db/                      # 数据库模型与迁移脚本（SQLite）
│   │   ├── models.js            # 链接与批次的数据模型定义
│   │   └── migrations/          # 版本迁移文件（按时间戳命名）
│   └── utils/                   # 通用工具函数
│       ├── urlValidator.js      # URL 格式校验与协议白名单过滤
│       └── logger.js            # 结构化日志输出（支持 JSON 格式）
├── config/                      # 环境配置目录
│   ├── default.json             # 默认配置（端口、数据库路径、超时阈值）
│   └── production.json          # 生产环境覆盖配置
├── docs/                        # 完整技术文档与架构说明
│   ├── deployment.md
│   ├── api/
│   └── batch-management.md
├── test/                        # 单元测试与集成测试用例
│   ├── unit/
│   └── integration/
├── scripts/                     # 运维辅助脚本
│   ├── seed-batch-28.js         # 导入第 28 批次示例数据的脚本
│   └── healthcheck.sh           # 服务存活检查脚本
├── .env.example                 # 环境变量示例文件
├── Dockerfile                   # 多阶段构建文件（基于 Alpine）
├── package.json                 # 项目依赖声明与脚本入口
└── README.md                    # 本文档
```

## 贡献指南

1. 查阅 Issue 列表，认领未被分配的 Feature 或 Bug 标签任务，或新建 Issue 描述你发现的问题或建议的新功能。
2. 从 `main` 分支拉取最新的开发分支，分支命名遵循 `feature/功能简述` 或 `fix/问题简述` 格式。
3. 在本地环境完成代码修改后，执行 `npm run test` 确保所有现有测试通过，并为新增逻辑编写至少一个正向测试用例。
4. 提交代码时遵循 Conventional Commits 规范（如 `feat: 增加链接批量导入重试机制`），并附带清晰的 commit body 描述变更理由。
5. 发起 Pull Request 至 `main` 分支，PR 描述中需关联对应的 Issue 编号，并附上本地自测截图或日志片段。至少需一名项目维护者审阅通过方可合并。

## 常见问题

**问：导入链接时提示“URL 重复”，但我的确需要再次收录同一个地址，应该如何处理？**

答：系统默认启用严格去重策略，以 URL 完整字符串的 SHA-256 哈希作为唯一键。若确认需强制重复收录，可在导入 API 的请求体中添加 `force: true` 字段，系统将跳过去重检查并创建新记录，同时保留旧记录。请注意，这会导致同一个链接在批次列表中多次出现，建议仅在特殊场景下使用。

**问：系统能否自动抓取目标页面的正文内容或截图？**

答：不能。LinkVault 的设计目标明确为“只存链接，不存内容”。系统仅会在导入时向目标域名发送一个轻量级 HEAD 请求，用以获取响应头中的 `Content-Type` 和 `Content-Length`，以及解析返回 HTML 中的 `<title>` 标签。我们强烈建议用户不要试图扩展此行为，以避免潜在的法律风险及目标服务器的访问压力。若需要全文检索或快照功能，建议搭配专用的网页归档工具使用。

## 许可证

MIT

> 外链数量: 10 | 生成时间: 2026-08-14 21:24:15
