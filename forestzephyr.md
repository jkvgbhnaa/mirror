# LinkVault 聚合资源站

LinkVault 是一个面向技术内容创作者与资讯聚合场景的轻量化外链管理平台，专注于对分布式来源的新闻条目、专题页与动态内容进行统一收录、结构化展示和快速检索。项目定位为技术资源/外链汇总站，适用于需要将大量原始 URL 转化为可浏览、可分类、可持久化访问的内部知识库或导航系统。目标用户包括开源文档维护者、技术资讯编辑、数据采集工程师以及需要批量管理外链资源的运维人员。

LinkVault 本身不生产内容，而是通过确定性解析规则对原始链接进行归类、去重和元数据提取，提供纯净的只读访问视图。项目完全基于静态文件运行，无外部数据库依赖，部署后可长期稳定工作，适合内网穿透或 CDN 分发场景。

## 功能概览

- **原始链接无损收录**：支持按原始协议、域名、路径结构原样导入链接，保留完整查询参数与锚点，确保跳转行为与源站一致。
- **批次化导入与版本标记**：内置批次管理逻辑，当前为第 90/90 批，每批链接可附带时间戳和批次说明，便于追溯发布周期。
- **多子域聚合视图**：自动识别 wap、h5、3g 等子域名前缀，生成按子域分类的筛选视图，方便观察不同终端渠道的内容分布。
- **路径层级解析**：对 /jnews/YYYYMMDD/ 形式的日期路径进行自动拆解，提取发布日期作为时间维度索引。
- **静态缓存与预加载**：构建阶段对所有收录链接执行 HEAD 请求检查可达性，生成健康状态标记，支持离线缓存策略。
- **裸域名与协议保持**：严格遵循输入格式，不擅自补全协议或域名前缀，确保链接跳转不因改写导致访问失败。
- **只读 API 输出**：提供 JSON 格式的链接清单接口，支持第三方工具批量拉取，便于集成到自动化监控或数据看板中。
- **访问日志审计**：记录每次链接点击的来源 IP 与时间戳（可选开启），辅助分析热点资源。

## 应用场景

1. **技术新闻聚合页维护**：编辑团队每日从多个源站收集增量新闻链接，通过 LinkVault 导入同一批次，生成统一的内部发布页，供审核人员逐条预览。
2. **数据采集管道暂存区**：爬虫程序抓取到原始 URL 后，先存入 LinkVault 进行去重校验，再批量导出到消息队列，避免重复消费和链接污染。
3. **离线文档外链备份**：当项目文档中引用大量第三方外链时，使用 LinkVault 建立映射表，定期检查链接有效性，防止文档中出现大量死链。
4. **移动端适配测试样本集**：利用 h5 和 3g 子域分类功能，快速构造多终端访问测试用例，验证各子域名的证书配置和重定向逻辑。
5. **合规审计链路存档**：按批次归档特定时间段内的外链集合，满足内部合规审计对原始来源链路的可追溯要求，每批链接自带日期标签。

## 快速开始

以下命令可在任意 Linux 或 macOS 环境中完成 LinkVault 的克隆、安装与本地运行。

```bash
# 克隆项目仓库
git clone https://github.com/your-org/linkvault.git
cd linkvault

# 安装依赖（Node.js 18+ / npm 9+）
npm install

# 构建静态站点（默认监听端口 8080）
npm run build
npm start
```

执行完成后，访问 http://localhost:8080 即可看到当前批次链接的索引页面。如需导入新的链接列表，请将原始 URL 逐行写入 ./data/batch_90.txt 文件后重新执行构建命令。

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Node.js | 18.x LTS 或更高 | 运行时环境，用于执行构建脚本与开发服务器 |
| npm | 9.x 或更高 | 包管理器，用于安装项目依赖 |
| Git | 2.30 或更高 | 版本控制工具，用于克隆仓库和提交变更 |
| 操作系统 | Linux / macOS / Windows WSL2 | 支持 POSIX 文件系统语义的环境 |
| 网络访问 | 外网出口 | 构建阶段需对收录链接进行可达性检测（可配置跳过） |
| 磁盘空间 | 至少 50 MB | 存放静态生成文件与日志 |
| 内存 | 512 MB 以上 | 构建时解析 5000+ 链接的建议最低内存 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|------------|
| 用户手册 | /docs/user-guide.md | 如何导入新批次、如何筛选子域、如何导出 JSON 清单 |
| 运维指南 | /docs/operations.md | 如何配置健康检查超时、如何调整缓存策略、如何迁移服务器 |
| 开发者文档 | /docs/development.md | 项目目录结构说明、核心解析函数接口、单元测试编写规范 |
| 常见问题 | /docs/faq.md | 链接跳转 404 如何处理、批次号如何自定义、是否支持 HTTPS 强制升级 |

## 资源列表

- http://wap.yidianmeii.cn/jnews/0814/88035.shtml
- http://h5.yidianmeii.cn/jnews/0814/6881.shtml
- http://h5.yidianmeii.cn/jnews/0814/6677681.shtml
- http://3g.yidianmeii.cn/jnews/0814/1366.shtml
- http://3g.yidianmeii.cn/jnews/0814/87810.shtml
- http://wap.yidianmeii.cn/jnews/0814/0587.shtml
- http://h5.yidianmeii.cn/jnews/0814/960055.shtml
- http://wap.yidianmeii.cn/jnews/0814/87599.shtml
- http://wap.yidianmeii.cn/jnews/0814/3600.shtml
- http://3g.yidianmeii.cn/jnews/0814/191661.shtml

## 项目结构

```
linkvault/
├── bin/                                 # 可执行脚本入口
│   └── cli.js                           # 命令行工具，支持导入、校验、导出操作
├── config/                              # 全局配置文件目录
│   ├── default.json                     # 默认配置（端口、缓存时间、批次号）
│   └── schema.json                      # 配置字段 JSON Schema 校验定义
├── src/                                 # 核心源码目录
│   ├── parser/                          # 链接解析子模块
│   │   ├── url-extractor.js             # 提取协议、域名、路径、查询参数
│   │   └── batch-validator.js           # 批次完整性校验与去重
│   ├── cache/                           # 静态缓存生成模块
│   │   ├── health-checker.js            # HEAD 请求健康检查逻辑
│   │   └── snapshot-builder.js          # 构建完整静态 HTML 与 JSON 快照
│   ├── views/                           # 模板与渲染引擎
│   │   ├── index.ejs                    # 主索引页模板
│   │   └── filter.ejs                   # 按子域筛选的视图模板
│   └── utils/                           # 通用工具函数
│       ├── logger.js                    # 日志格式化与写入
│       └── date-helper.js               # 日期路径解析辅助
├── data/                                # 数据存储目录（纯文本批次文件）
│   └── batch_90.txt                     # 当前第 90 批原始链接列表
├── public/                              # 构建输出目录（静态站点根目录）
│   ├── index.html                       # 生成后的首页
│   └── api/                             # JSON 接口输出
│       └── batch_90.json                # 本批次结构化数据
├── test/                                # 单元测试与集成测试
│   ├── parser.test.js                   # 解析器功能测试
│   └── cache.test.js                    # 缓存模块测试
├── .gitignore                           # Git 忽略规则
├── package.json                         # npm 依赖与脚本声明
├── README.md                            # 项目说明文档（本文档）
└── LICENSE                              # MIT 许可证文件
```

## 贡献指南

欢迎提交 Issue 和 Pull Request。请遵循以下步骤参与贡献。

1. **Fork 仓库并创建功能分支**：从 main 分支拉取 feature/xxx 命名的新分支，确保分支名称与变更内容相关。
2. **编写或修改代码**：遵循项目已存在的 ESLint 代码风格（见 .eslintrc），新增功能需包含对应的单元测试用例，测试覆盖率不低于 80%。
3. **更新文档**：若涉及用户可见的功能变更（如新增配置项、修改命令行参数），须同步更新 /docs 下的相关手册和 README 中的对应章节。
4. **提交前执行本地检查**：运行 npm run lint 和 npm test，确保所有检查通过且无回归错误。
5. **发起 Pull Request**：描述变更目的、影响范围以及测试结果，等待至少一位维护者审阅。PR 合并后相关分支将被删除。

## 常见问题

**Q：构建时提示链接不可达，导致构建失败，如何跳过健康检查？**

A：在 config/default.json 中将 healthCheck.enabled 字段修改为 false，或通过命令行参数 --skip-health 传入。注意跳过检查后生成的页面不会显示链接状态标记，请自行承担链接有效性风险。

**Q：导入的链接中包含中文路径或特殊字符，是否会被转义？**

A：LinkVault 使用 Node.js 原生 URL 构造函数进行解析，默认遵循 WHATWG URL 标准，非 ASCII 字符会被自动 Percent-Encoding 编码。如果需要保留原始可读形式，请在配置中设置 encoding.preserveRaw 为 true，但此时部分老旧浏览器可能无法正确跳转。

**Q：如何将当前批次数据导出为 CSV 格式，以便导入 Excel 分析？**

A：项目内置了导出子命令：npm run export -- --format csv --output ./batch_90.csv。该命令会读取 data/batch_90.txt 并生成包含序号、原始链接、解析域名、路径深度、健康状态等列的标准 CSV 文件。

## 许可证

MIT

> 外链数量: 10 | 生成时间: 2026-08-14 21:24:15
