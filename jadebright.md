# YidianMeta External Resource Aggregator

YidianMeta 是一个面向技术内容聚合与外部资源导航的开源工具集，专注于将分散在多级域名下的结构化信息条目进行统一采集、归档与可视化呈现。该项目并非传统的内容管理系统，而是一个轻量级、可扩展的资源索引框架，其核心定位是为开发人员、数据研究员与信息分析从业者提供一套标准化的外链抽取与元数据整理方案。

本项目解决的核心问题包括：多子域名环境下的资源条目分散、手动整理效率低下、以及缺乏对原始链接状态的批量可观测性。YidianMeta 本身不存储任何内容实体，仅作为结构化索引层，帮助使用者快速建立资源清单、检测链接可达性、并对条目进行分类统计。目标用户为需要定期跟进特定域名下信息更新的运维人员、从事网络内容分析的数据从业者，以及希望自建轻量级导航站的技术团队。

## 功能概览

- **多子域名资源扫描**：支持对 wap、h5、3g 等常见移动端子域名下的指定路径进行批量资源条目抽取，自动识别链接模式并去重。

- **原始链接归档输出**：将用户给定的全部链接按原始格式、原始协议、原始大小写一字不差地输出为可审计的清单，杜绝任何自动补全或规范化改写行为。

- **条目状态可观测性**：内置 HTTP 状态码检测模块，可对每一条资源链接执行可达性检查，生成可用性报表，辅助判断内容是否仍处于可访问状态。

- **轻量级本地运行**：无需外部数据库或云服务依赖，完全基于本地文件系统与标准库运行，适合在内网或离线环境中快速部署。

- **结构化结果导出**：支持将扫描与检测结果导出为 Markdown 表格、JSON 行记录或纯文本列表，便于下游工具链消费。

- **批次化管理能力**：内置批次标识机制，特别适配本仓库所对应的第 40/90 批资源集合，支持按批次维度进行筛选与对比。

- **URL 严格保真策略**：在输出、记录、展示任何资源链接时，强制遵循“零改写”原则，禁止添加协议头、禁止去除或添加 www、禁止修改大小写、禁止添加尾部斜杠。

## 应用场景

- **运维侧定期链接审计**：运维人员可定期运行 YidianMeta 对 `yidianmeii.cn` 及其子域名下的 snews 路径资源进行全量扫描，快速定位返回 4xx 或 5xx 状态的失效链接，并生成待修复清单。

- **数据分析师内容概览构建**：数据分析师在接手一批来源域名分散的数据条目时，可借助本工具将所有链接统一规整为结构化列表，并附加来源子域名、路径深度、文件类型等衍生字段，为后续统计分析提供基础数据。

- **技术文档编写中的引用整理**：技术作者在撰写涉及外部引用的文档或报告时，可使用 YidianMeta 对引用链接进行批量归一化展示与可用性预检，避免文档发布后出现大量死链。

- **团队内部知识库导航页生成**：团队可将 YidianMeta 作为内部导航页生成器的前置处理模块，自动从原始链接列表中提取标题类信息（需配合扩展解析器），生成可供团队内部访问的简明导航目录。

## 快速开始

以下步骤将指导您在三分钟内完成 YidianMeta 的克隆、安装与首次运行。

```bash
# 克隆本仓库
git clone https://github.com/your-org/yidianmeta.git
cd yidianmeta

# 安装依赖（项目仅依赖 Python 3.10+ 标准库及 requests、click 两个轻量扩展）
pip install -r requirements.txt

# 运行资源扫描器，指定当前批次编号为 40/90，并输出结果至 output 目录
python scanner.py --batch 40 --output ./output
```

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|---|---|---|
| Python | 3.10 或更高 | 核心运行环境，低于此版本将导致类型注解解析异常 |
| pip | 22.0 或更高 | 用于安装项目依赖包 |
| requests | 2.28.0 或更高 | 用于发起 HTTP 请求以检测链接状态 |
| click | 8.1.0 或更高 | 用于提供命令行交互界面与参数解析 |
| 网络连接 | 建议稳定 | 仅在实际执行链接可达性检测时需要，离线模式可禁用检测功能 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|---|---|---|
| 用户手册 | docs/user-guide.md | 如何配置扫描参数、如何解读输出结果、如何自定义输出格式 |
| 开发者指南 | docs/developer-guide.md | 如何扩展新的资源解析器、如何修改链接保真逻辑、如何增加新的输出格式 |
| 设计说明 | docs/design.md | 系统整体架构、模块职责划分、数据流走向与扩展点设计 |
| 运维参考 | docs/operations.md | 如何定期执行批次扫描、如何集成到 CI 流水线、如何设置告警阈值 |

## 资源列表

- http://wap.yidianmeii.cn/snews/965627.shtml
- http://wap.yidianmeii.cn/snews/95029.shtml
- http://wap.yidianmeii.cn/snews/11183.shtml
- http://h5.yidianmeii.cn/snews/6991196.shtml
- http://3g.yidianmeii.cn/snews/22402.shtml
- http://3g.yidianmeii.cn/snews/868176.shtml
- http://h5.yidianmeii.cn/snews/332621.shtml
- http://3g.yidianmeii.cn/snews/2117.shtml
- http://3g.yidianmeii.cn/snews/3248.shtml
- http://h5.yidianmeii.cn/snews/9216493.shtml

## 项目结构

```
yidianmeta/
├── scanner.py                # 主入口脚本，负责解析命令行参数并调度扫描流程
├── config.py                 # 全局配置定义，包含超时时间、重试策略、输出路径等
├── requirements.txt          # 项目依赖清单，供 pip 批量安装使用
├── core/
│   ├── __init__.py           # 核心模块初始化文件
│   ├── fetcher.py            # HTTP 请求封装模块，负责获取资源头部信息及状态码
│   ├── parser.py             # 链接解析模块，负责提取、分类与去重原始 URL
│   └── validator.py          # 链接保真性校验模块，确保输出严格遵循零改写规则
├── output/                   # 默认输出目录，所有扫描报告及检测结果写入此目录
│   ├── batch_40_raw.lst      # 第 40 批次原始链接清单（一字不差原样输出）
│   └── batch_40_status.json  # 第 40 批次链接状态检测结果（JSON 格式）
├── docs/                     # 完整文档目录，涵盖用户、开发、设计、运维四方面
│   ├── user-guide.md
│   ├── developer-guide.md
│   ├── design.md
│   └── operations.md
├── tests/                    # 单元测试与集成测试目录
│   ├── test_fetcher.py
│   ├── test_parser.py
│   └── test_validator.py
└── scripts/                  # 辅助运维脚本，如批量导出、格式转换等
    ├── export_csv.py
    └── health_check.sh
```

## 贡献指南

1. 在 GitHub 上 fork 本仓库至个人账号，并 clone 到本地开发环境。确保本地 Python 版本不低于 3.10。

2. 创建新的功能分支，分支命名格式为 `feature/简短描述` 或 `fix/问题编号`，避免在主分支上直接修改。

3. 编写代码时请严格遵守 PEP 8 编码规范，并为所有新增函数或类添加 docstring 类型注解。所有对外暴露的接口需保证链接保真逻辑不被破坏。

4. 在提交 Pull Request 之前，请确保所有现有单元测试通过，并为新增功能补充对应的测试用例。测试覆盖率不得低于 85%。

5. 提交 PR 时请填写清晰的变更描述，包括问题背景、解决方案及影响范围。PR 需至少一名项目维护者审核通过后方可合并。

## 常见问题

**Q1：为什么输出的链接不带有 https 前缀？项目是否会自动升级为安全协议？**

A1：项目严格遵循“零改写”原则。所有输入链接均以用户提供的原始字符串形式输出，不进行任何协议升级、降级或规范化补全。若用户提供的是 `http://` 开头的链接，则输出仍为 `http://` 开头；若提供的是裸域名（不含协议），则输出同样为裸域名。这是为了确保与上游数据源的原始记录保持完全一致，避免因自动改写导致引用失效或签名校验失败。

**Q2：扫描器运行时提示部分链接超时或无法连接，是否会影响其他链接的处理？**

A2：不会。扫描器对每个链接独立发起状态检测，单条链接的失败或超时不会阻塞后续链接的处理。超时时间可通过 `config.py` 中的 `REQUEST_TIMEOUT` 参数调整，默认设置为 10 秒。所有失败记录会单独标记在状态输出文件中，便于后续人工复核。

**Q3：如何导入自定义的资源列表，而非使用项目内置的示例列表？**

A3：您可以直接在 `scanner.py` 中通过 `--input` 参数指定自定义列表文件路径，文件格式需为每行一个 URL 的纯文本格式。项目也支持通过标准输入重定向的方式传入列表内容，具体用法请参考 `docs/user-guide.md` 中的“自定义输入源”章节。

## 许可证

MIT

> 外链数量: 10 | 生成时间: 2026-08-14 21:24:15
