# YidianMeii News Aggregator

YidianMeii News Aggregator 是一个轻量级的技术资讯与新闻外链聚合平台，专为开发者、技术决策者及信息密集型工作者设计。该项目不产生原创内容，而是通过结构化索引和智能分类，将分散于多端（移动端、WAP 端、H5 端）的每日快讯进行统一收纳与快速呈现，解决用户在多源信息流中重复切换、检索效率低下的问题。本仓库作为第 57/90 批次资源整合的入口，集中管理 2026 年 8 月 14 日当日发布的十篇重点新闻外链，并提供统一的元数据标注与访问路由。

## 功能概览

- **多端适配链接聚合**：自动识别并归拢 wap、3g、h5 三种终端前缀的新闻链接，确保在不同设备环境下均可获得最佳阅读体验。
- **批次化资源管理**：采用批次（Batch）与日期（Date）双维度组织方式，当前批次为 57/90，日期锁定为 0814，便于历史回溯与周期复盘。
- **轻量级元数据提取**：从链接本身解析出新闻标识符（即 shtml 文件名中的纯数字部分），为后续的标题抓取或摘要生成预留扩展接口。
- **静态外链跳转中枢**：项目本身不存储任何新闻正文，仅作为可信跳转白名单，降低本地存储压力并规避版权风险。
- **健康状态探测**（规划中）：集成简单的 HTTP 状态码检查，标记失效链接或重定向链路，提升资源可用性透明度。
- **快速部署与内网共享**：支持一键 Clone 后在任意静态服务器（Nginx/Apache/Node.js serve）中运行，适合团队内部知识库挂载。
- **可扩展的目录脚手架**：预留了 `parser/`、`validator/`、`reporter/` 等模块目录，方便二次开发时添加自定义处理流水线。

## 应用场景

- **日常早报速览**：技术团队负责人或开发者在每日晨会前，通过本聚合页一次性浏览当天所有重要资讯链接，替代逐一打开多个标签页或收藏夹的繁琐操作。
- **运维故障信息回溯**：当某条新闻链接指向的源站出现临时维护或内容更新时，运维人员可利用本仓库的批次记录快速定位到对应日期的资源列表，并与源站日志进行交叉比对。
- **内容审核与安全巡检**：合规部门或安全审计人员可定期拉取本仓库，对批次内的外链进行存活性和内容合规性抽查，由于所有链接均以纯文本列表形式暴露，便于编写自动化扫描脚本。
- **离线归档与快照索引**：数据归档工程师可将本仓库作为索引文件（Index File），配合 wget 或 httrack 工具对列表中的 URL 进行批量离线保存，形成带时间戳的本地镜像目录。

## 快速开始

以下命令演示了如何将本项目克隆至本地，安装基础依赖（如有），并启动一个极简的开发服务器用于预览链接列表。

```bash
# 1. 克隆仓库
git clone https://github.com/your-org/yidianmeii-aggregator.git
cd yidianmeii-aggregator

# 2. 安装依赖（本项目核心仅为静态 HTML + Markdown，如需运行内置验证工具需安装 Node.js 依赖）
npm install --global serve   # 或使用 Python3 自带的 http.server

# 3. 启动服务
serve -p 3000                # 使用 serve 启动
# 或者
python3 -m http.server 8080  # 使用 Python 启动
```

启动后，在浏览器中访问 `http://localhost:3000` 或 `http://localhost:8080` 即可看到 `index.html` 渲染的链接列表。若需修改或增删链接，直接编辑本 README 中的「资源列表」章节，并重新生成静态页面即可。

## 安装要求

| 依赖项 | 必需性 | 说明 |
|--------|--------|------|
| Git | 必需 | 用于克隆仓库及版本控制，最低版本 2.20.0 |
| Node.js (可选) | 推荐 | 若使用 serve 或自定义验证脚本，需 Node.js 14.x 及以上版本 |
| Python 3.x (可选) | 推荐 | 若不安装 Node.js，可使用 Python 内置的 http.server 模块 |
| 现代浏览器 | 必需 | 推荐 Chrome 85+ / Firefox 78+ / Edge 88+，用于渲染和点击链接 |
| 网络访问 | 必需 | 需能够访问外网以加载 `yidianmeii.cn` 域名下的新闻资源 |
| 静态服务器 (Nginx/Apache) | 可选 | 生产环境部署时建议使用专业 Web 服务器，开发阶段可使用轻量级工具 |
| 文本编辑器 (VSCode/Vim) | 推荐 | 用于编辑资源列表或修改项目配置 |
| make (可选) | 可选 | 若使用 Makefile 自动化任务，需 GNU Make 3.81+ |

## 文档导航

| 层面 | 目录/章节 | 回答的问题 |
|------|-----------|------------|
| 用户入门层 | 快速开始 + 安装要求 | 如何最快速度启动本项目并看到链接列表？我的环境是否满足运行条件？ |
| 资源管理层 | 资源列表 | 本批次包含哪些具体的新闻外链？它们的原始 URL 是什么？ |
| 开发扩展层 | 项目结构 + 贡献指南 | 项目的目录是如何组织的？我想添加新的解析模块或验证逻辑应该从哪里入手？ |
| 运维排错层 | 常见问题 | 遇到链接打不开、页面乱码或端口冲突时如何解决？ |

## 资源列表

- http://wap.yidianmeii.cn/vnews/0814/6983.shtml
- http://3g.yidianmeii.cn/vnews/0814/0722.shtml
- http://h5.yidianmeii.cn/vnews/0814/7812255.shtml
- http://3g.yidianmeii.cn/vnews/0814/7741.shtml
- http://wap.yidianmeii.cn/vnews/0814/1164004.shtml
- http://3g.yidianmeii.cn/vnews/0814/83900.shtml
- http://h5.yidianmeii.cn/vnews/0814/6680444.shtml
- http://3g.yidianmeii.cn/vnews/0814/5161461.shtml
- http://3g.yidianmeii.cn/vnews/0814/0114069.shtml
- http://wap.yidianmeii.cn/vnews/0814/9411089.shtml

## 项目结构

项目采用模块化分层设计，核心代码与资源配置严格分离，以便于批次迭代和多人协作。以下为当前主干目录树及其职责说明：

```
yidianmeii-aggregator/
├── README.md                      # 项目入口文档，包含简介、快速开始及完整资源列表（即当前文件）
├── index.html                     # 静态渲染入口，将资源列表转换为可点击的卡片式布局，适配移动端
├── config/
│   ├── batch.json                 # 记录当前批次号（57）、总批次数（90）、发布日期（2026-08-14）
│   └── whitelist.conf            # 预留的域名白名单配置，用于验证外链是否属于允许跳转的范围
├── src/
│   ├── core/
│   │   ├── fetcher.js            # 封装 fetch 请求，用于批量检查链接状态（200/404/301）
│   │   └── parser.js             # 从原始 URL 中提取新闻 ID、终端类型及文件扩展名
│   ├── validators/
│   │   ├── urlValidator.js       # 校验 URL 协议、域名格式及路径合法性
│   │   └── batchValidator.js     # 校验当前批次内的链接数量是否与配置中声明的一致
│   └── reporters/
│       ├── consoleReporter.js    # 将验证结果输出到终端，支持彩色日志
│       └── htmlReporter.js       # 生成带有状态标记的 HTML 报告，替换 index.html
├── assets/
│   ├── styles/
│   │   └── main.css              # 极简风格的响应式样式表，适配深色/浅色模式
│   └── scripts/
│       └── clipboard.js          # 一键复制所有链接的辅助脚本（可选功能）
├── tests/
│   ├── unit/
│   │   ├── parser.test.js        # 单元测试：验证 parser 对各类 URL 的分割正确性
│   │   └── validator.test.js     # 单元测试：验证白名单和数量校验逻辑
│   └── fixtures/
│       └── sample-urls.txt       # 模拟历史批次的 URL 样本，用于回归测试
├── scripts/
│   ├── deploy.sh                 # 生产环境部署脚本（打包、压缩、上传至 CDN）
│   └── cron-check.sh             # 每日定时任务脚本，自动探测列表中的链接存活率
├── .gitignore                    # 忽略 node_modules、.env、临时日志等
├── package.json                  # Node.js 项目描述文件，定义依赖与脚本命令
└── LICENSE                       # MIT 许可证全文
```

## 贡献指南

我们欢迎任何形式的贡献，包括但不限于新增链接批次、修复验证逻辑、完善文档或提交测试用例。请遵循以下标准流程：

1.  **Fork 本仓库并创建特性分支**：从 `main` 分支切出新的分支，命名规范为 `feature/batch-58` 或 `fix/url-parser-issue`，确保分支名具有描述性。
2.  **更新资源列表或修改代码**：若为新增批次，请在「资源列表」章节末尾追加新的链接，并同步更新 `config/batch.json` 中的批次号和日期。若为代码修复，请确保新增或修改的代码附带对应的单元测试。
3.  **执行本地验证**：运行 `npm test` 或 `make test` 以确认所有单元测试通过，并手动启动开发服务器检查 `index.html` 渲染结果是否与预期一致。
4.  **提交 Pull Request**：提交时请详细填写 PR 模板，说明本次变更的目的、影响范围以及测试覆盖情况。PR 标题需包含 [Batch Update] 或 [Core Fix] 前缀。
5.  **代码审查与合并**：至少一位项目维护者将对 PR 进行审查，如有改动意见将标记为 `needs-changes`，待修改完成后由维护者执行 Squash Merge。

## 常见问题

**Q1：资源列表中的某些链接无法访问，返回 404 或连接超时，应如何处理？**

A：这通常是由于源站内容归档或临时网络波动导致。首先请尝试在浏览器中直接访问该 URL，确认是否真的失效。若确认失效，请在本仓库的 Issues 中提交「链接失效」报告，并附上批次号和具体 URL。项目维护者会在下一个批次更新中将其移除或替换为有效镜像。在紧急情况下，您也可以自行 Fork 仓库并编辑「资源列表」章节，删除或注释掉该链接。

**Q2：为什么项目不直接将新闻标题或正文抓取下来，而是只提供外链？**

A：本项目定位为「聚合索引」而非「内容存储」。直接抓取并托管第三方新闻正文会涉及版权合规风险，且大幅增加存储与带宽成本。通过纯外链形式，用户可以始终访问源站的最新内容（包括后续的更正或更新），同时我们的验证模块（`validators/`）会在后台持续监控链接状态，兼顾了可用性与合规性。

**Q3：我想将本仓库部署到公司内网，但无法访问外网 CDN 资源（如字体、公共样式库），该如何配置？**

A：本项目已默认将所有静态资源（CSS、JS）放置于 `assets/` 目录下，不依赖任何外部 CDN。您只需将整个仓库克隆到内网服务器，并确保内网客户端能够解析 `yidianmeii.cn` 域名的 DNS（或在内网 Hosts 中绑定），即可完全在内网环境中运行。若内网完全隔离外网，则链接本身将无法跳转，此时建议配合内网代理或镜像站点使用。

## 许可证

MIT

> 外链数量: 10 | 生成时间: 2026-08-14 21:24:15
