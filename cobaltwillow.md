# YDMNews-Index

YDMNews-Index 是一个面向移动端新闻资讯聚合场景的轻量化外链导航与资源索引系统。该项目主要服务于需要快速整合、分类、展示来自多个内容源新闻链接的开发者、内容运营人员及个人站长，帮助其降低多源内容管理成本，提升信息分发效率。项目本身不存储任何新闻内容，仅作为结构化索引层，通过可配置的链接映射与分类机制，将分散的第三方新闻页面统一收敛至同一入口，适用于构建新闻推荐页、热点聚合栏或内部信息看板。

## 功能概览

- 多源链接聚合管理：支持将不同子域名下的新闻链接统一纳入索引，通过配置文件灵活增删数据源，无需修改核心代码。
- 分类与标签筛选：内置分类字段，可根据新闻主题、发布时间或来源站点对链接进行分组，并支持按标签快速过滤。
- 响应式卡片布局：面向移动端优先设计，在手机、平板及桌面端均能自适应展示链接卡片，保证点击区域友好且阅读体验一致。
- 链接状态健康检查：后台定时任务可对已收录链接进行可达性探测，自动标记失效或响应超时的链接，便于运营人员及时清理。
- 缓存与刷新策略：支持 Redis 或内存缓存索引数据，减少重复解析开销；同时提供手动刷新接口，方便内容更新后即时生效。
- 基础访问统计：记录每个外链的点击次数与最后访问时间，为内容热度分析提供原始数据支持。
- 开放数据导出：支持将当前索引数据导出为 JSON、CSV 格式，便于对接第三方分析工具或进行线下备份。

## 应用场景

- 地方资讯站每日热点汇总：运营人员每日从合作媒体获取新闻链接，通过 YDMNews-Index 统一分类并生成热点列表页，用户可一站式浏览当日本地要闻，无需逐个访问多个媒体站点。
- 企业内部信息聚合看板：企业行政或 IT 部门可将公司内部公告、行业动态、政策解读等链接集中收录，构建仅供内网访问的信息导航页，方便员工快速获取最新通知。
- 个人兴趣内容聚合：个人开发者或博主可将自己关注的科技、体育、娱乐等领域 RSS 或网页链接整理入系统，生成个人定制的阅读入口，避免被社交平台算法干扰信息获取。

## 快速开始

以下步骤适用于 Linux / macOS / Windows WSL 环境，确保系统已安装 Git、Node.js（v16 或以上）及 npm。

```bash
# 克隆项目仓库
git clone https://github.com/your-org/ydmnews-index.git
cd ydmnews-index

# 安装项目依赖
npm install

# 复制环境变量示例文件并填入必要配置（如端口、缓存方式）
cp .env.example .env

# 启动开发服务器（默认监听 3000 端口）
npm run dev
```

启动后访问 `http://localhost:3000` 即可看到默认的新闻索引页面。如需修改链接数据，请编辑 `data/sources.json` 文件并重启服务或调用刷新接口。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---------|---------|------|
| Node.js | >= 16.0.0 | 项目运行时环境，用于执行服务端代码与构建脚本 |
| npm | >= 8.0.0 | 依赖管理与脚本执行工具，随 Node.js 一同安装 |
| Redis（可选） | >= 6.0 | 用于启用缓存与会话存储；如不配置则使用内存缓存 |
| SQLite（开发） | 3.x | 默认数据库，用于存储链接元数据与统计信息，无需额外安装 |
| PM2（生产） | >= 5.0 | 生产环境进程守护工具，推荐使用以保持服务持续运行 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|-----------|
| 入门指南 | `docs/quick-start.md` | 如何最快在本地跑起来？如何修改首页展示的链接？ |
| 配置参考 | `docs/configuration.md` | 环境变量有哪些？数据源 JSON 结构如何定义？缓存如何设置？ |
| 部署手册 | `docs/deployment.md` | 如何部署到 VPS、云服务器或容器平台？如何启用 HTTPS？ |
| API 文档 | `docs/api.md` | 提供了哪些内部接口供二次开发？如何获取 JSON 格式的索引数据？ |

## 资源列表

- http://wap.yidianmeii.cn/jnews/0814/9111636.shtml
- http://3g.yidianmeii.cn/jnews/0814/8068.shtml
- http://wap.yidianmeii.cn/jnews/0814/115539.shtml
- http://wap.yidianmeii.cn/jnews/0814/8311733.shtml
- http://3g.yidianmeii.cn/jnews/0814/6660.shtml
- http://wap.yidianmeii.cn/jnews/0814/86001.shtml
- http://h5.yidianmeii.cn/jnews/0814/691871.shtml
- http://wap.yidianmeii.cn/jnews/0814/8116.shtml
- http://wap.yidianmeii.cn/jnews/0814/6695.shtml
- http://3g.yidianmeii.cn/jnews/0814/7191.shtml

## 项目结构

```
ydmnews-index/
├── src/                           # 核心源代码目录
│   ├── controllers/               # 控制器层，处理请求与响应逻辑
│   ├── services/                  # 业务服务层，包含链接解析、分类、缓存等核心功能
│   ├── models/                    # 数据模型定义，包含链接实体、分类、点击记录等
│   ├── middleware/                # 中间件，含鉴权、日志、跨域、错误拦截等
│   └── utils/                     # 工具函数，如日期格式化、URL 校验、配置读取等
├── data/                          # 数据存储目录
│   ├── sources.json               # 默认链接数据源配置文件（可热更新）
│   └── db/                        # SQLite 数据库文件存放位置（生产环境建议迁移至外部）
├── public/                        # 静态资源目录
│   ├── css/                       # 构建后的样式文件
│   ├── js/                        # 构建后的前端脚本
│   └── assets/                    # 图片、字体等静态资源
├── views/                         # 服务端渲染模板（如有）
│   └── index.ejs                  # 首页模板，输出链接卡片列表
├── tests/                         # 单元测试与集成测试脚本
│   ├── unit/                      # 单元测试，覆盖核心工具函数与服务类
│   └── integration/               # 集成测试，覆盖 API 端点与数据流
├── docs/                          # 文档目录，包含快速开始、配置、部署、API 等详细说明
├── scripts/                       # 运维与辅助脚本
│   ├── health-check.js            # 链接健康检查定时任务脚本
│   └── data-migrate.js            # 数据迁移或导入导出工具
├── .env.example                   # 环境变量示例文件
├── .gitignore                     # Git 忽略规则
├── package.json                   # 项目依赖与脚本定义
├── README.md                      # 项目主文档（本文件）
└── LICENSE                        # MIT 许可证文件
```

## 贡献指南

1. 在 GitHub 上 fork 本仓库，并克隆到本地开发环境。建议创建一个新的功能分支进行开发，例如 `git checkout -b feature/add-category-filter`。
2. 确保代码风格与项目保持一致（使用 ESLint + Prettier 配置），在提交前运行 `npm run lint` 和 `npm test` 确保没有语法错误且所有测试用例通过。
3. 若新增功能或修改现有行为，请同步更新 `docs/` 目录下对应的文档文件，并在 `tests/` 中添加或更新测试用例。
4. 提交 commit 时请遵循 Conventional Commits 规范（如 `feat: 新增分类筛选接口`、`fix: 修复链接卡片点击统计错误`），便于自动生成变更日志。
5. 发起 Pull Request 到主仓库的 `main` 分支，在 PR 描述中清晰说明改动目的、实现方式以及影响范围，等待项目维护者审核。

## 常见问题

**问：启动后页面显示“无数据”，如何添加新闻链接？**

答：请检查 `data/sources.json` 文件是否存在且格式正确。该文件应包含一个数组，数组中每个对象至少包含 `title`（标题）、`url`（链接地址）和 `category`（分类）字段。修改文件后重启服务，或调用 `POST /api/reload` 接口热加载数据。如果文件无误但仍无显示，请查看控制台日志是否有解析错误提示。

**问：如何更新已收录链接的缓存？新增链接后为什么没有立即生效？**

答：默认缓存策略下，索引数据会被缓存 5 分钟。如需立即生效，可以手动调用 `POST /api/cache/clear` 接口清空缓存，或在配置文件中将 `CACHE_TTL` 设为 0 以禁用缓存（不推荐生产环境使用）。此外，也可以修改 `data/sources.json` 后直接调用 `/api/reload`，该接口会自动清除缓存并重新加载数据。

**问：生产环境如何配置 Redis 缓存？如果 Redis 连接失败会影响服务吗？**

答：在 `.env` 文件中设置 `REDIS_HOST`、`REDIS_PORT` 和 `REDIS_PASSWORD` 即可启用 Redis。当 Redis 连接失败时，服务会自动降级为内存缓存，并打印警告日志，不会导致进程崩溃。建议在生产环境使用 Redis 并配合 PM2 进程管理，确保高可用性。

## 许可证

MIT

> 外链数量: 10 | 生成时间: 2026-08-14 21:24:15
