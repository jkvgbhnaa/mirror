# LinkVault 聚合站

LinkVault 是一套面向技术团队与内容运营者的轻量级外链聚合与快照存档系统。该项目并非一个传统的 CMS 或博客框架，而是一个专为批量管理、归档和展示分布式资讯落地页设计的静态资源映射层。其核心定位在于帮助运维人员、爬虫调度员或数据审核员，将散落在不同移动端子域名下的动态新闻页（如 `/vnews/` 路径下的批量内容）统一收口至一套可阅读、可检索、可监控的目录体系中。项目自身不依赖数据库，所有链接映射关系基于结构化配置文件生成，既可作为内部数据看板的补充组件，也可作为对外输出合规链接清单的中间件。目标用户包括中小型资讯平台的技术维护人员、从事内容聚合的数据中间商，以及需要定期对大量落地页进行存活检查和质量抽检的运营专员。

## 功能概览

- **批量链接注入与自动归类**：系统提供一组 RESTful 风格的注入接口，允许用户按批次（如 90 批次）导入原始 URL 列表，引擎自动识别域名后缀（如 `wap`、`h5`、`3g`）并按设备类型标签进行分类存储，无需人工干预。

- **快照元数据提取**：每个入库的 URL 均会触发异步元数据抓取任务，包括标题预估、发布时间解析（基于路径中的日期段如 0814）、以及内容摘要的前 120 个字符截取，所有元数据存于本地 JSON 索引文件中。

- **静态目录树生成器**：基于注入的数据，系统可一键生成符合工程规范的 ASCII 目录结构（见项目结构章节），同时输出 `sitemap.json` 和 `index.html` 两种格式的导航入口，便于 Nginx 或 CDN 直接托管。

- **链接存活健康检查**：内置基于 `curl` 的轻量级探活模块，每隔 12 小时对库内全部链接发起 HEAD 请求，将状态码异常（非 2xx/3xx）的链接输出至 `deadlinks.log`，并支持自动重试 3 次。

- **分批导出与订阅机制**：支持按批次号（如 60/90）或日期前缀（如 0814）过滤导出链接清单，导出格式包括纯文本列表、Markdown 表格和 CSV。同时提供简单的 Webhook 回调，在每批链接入库完成后向指定监控群发送统计摘要。

- **访问频次与来源模拟**：为模拟真实用户访问行为，系统提供用户代理轮换池和随机延迟策略，供外部巡检脚本调用，避免在采集或预览过程中被源站策略屏蔽。

- **原始数据回显保护**：所有展示界面默认对原始链接进行 Base64 编码后再渲染，仅当用户通过特定参数（`?decoded=true`）且携带内部令牌时，才显示明文 URL，降低泄露风险。

## 应用场景

- **内部数据中台的落地页资源目录**：当公司拥有多个移动端子域名（如 `wap`、`h5`、`3g`）且每日产生大量动态新闻页时，数据团队可利用 LinkVault 将分散的链接集中管理，生成统一的资源清单，供下游推荐算法或风控模型调用。

- **合规审计前的链接预检**：在每批内容上线前，运营人员将待发布的链接批量导入 LinkVault，系统自动进行重复性检测、域名归属校验和敏感词模糊匹配，并将疑似问题链接高亮标记，从而在正式提交审核前完成一轮自动化过滤。

- **迁移或改版时的旧链接存档**：当资讯平台进行域名合并或路径重构时，可使用 LinkVault 对旧版所有 `vnews` 路径下的链接建立静态映射表，通过 301 重定向规则或前端路由表，确保历史书签和外链访问仍能正确跳转至新地址。

- **第三方内容采购的验单比对**：内容采购方从供应商处获得一批新闻链接后，可将清单导入 LinkVault，利用其批量元数据提取功能快速获取每条链接的标题和发布时间，与供应商提供的清单进行交叉比对，快速发现漏发或错发情况。

## 快速开始

以下步骤适用于 Linux/macOS 环境，确保系统已安装 Git、Node.js（v16+）和 npm。

```bash
# 克隆项目仓库
git clone https://github.com/your-org/linkvault.git
cd linkvault

# 安装核心依赖（解析引擎、文件监听、日志工具）
npm install

# 执行初始化配置，生成默认的 config.yaml 和空索引文件
npm run init

# 将当前批次的原始链接（第 60/90 批）注入系统
# 注：实际使用时应将链接列表放入 ./data/batch_60_90.txt，每行一个 URL
npm run inject -- --batch=60/90 --file=./data/batch_60_90.txt

# 生成静态目录结构与导航页面
npm run build

# 启动本地预览服务（默认监听 3000 端口）
npm start
```

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Node.js | v16.13.0 及以上 | 运行时环境，用于执行注入脚本、元数据抓取和构建流程 |
| npm | v8.0.0 及以上 | 包管理器，用于安装所有声明的第三方库（axios、js-yaml、chalk 等） |
| Git | v2.25.0 及以上 | 用于克隆仓库以及后续通过 hook 自动提交索引变更 |
| curl | v7.68.0 及以上 | 用于健康检查模块中的存活探测，替代 Node.js 原生 http 模块以支持更丰富的超时控制 |
| 磁盘空间 | 至少 200MB 空闲 | 用于存储索引 JSON、日志文件以及生成的静态页面，每万条链接约占用 15MB |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|------------|
| 运维部署 | `/docs/deployment.md` | 如何配置 Nginx 反向代理、设置 systemd 守护进程以及调整日志轮转策略 |
| 注入规范 | `/docs/injection-spec.md` | 原始链接文件的格式要求（编码、分隔符、字段映射）、批次编号规则以及去重策略 |
| 元数据配置 | `/docs/metadata-config.md` | 如何自定义摘要长度、用户代理列表、请求超时阈值以及代理服务器设置 |
| 扩展开发 | `/docs/extension-guide.md` | 如何编写自定义过滤器（如域名白名单）、如何替换默认的 JSON 索引为 SQLite 存储 |

## 资源列表

- http://wap.yidianmeii.cn/vnews/0814/88035.shtml
- http://h5.yidianmeii.cn/vnews/0814/4881.shtml
- http://h5.yidianmeii.cn/vnews/0814/6477681.shtml
- http://3g.yidianmeii.cn/vnews/0814/2366.shtml
- http://3g.yidianmeii.cn/vnews/0814/87820.shtml
- http://wap.yidianmeii.cn/vnews/0814/0587.shtml
- http://h5.yidianmeii.cn/vnews/0814/940055.shtml
- http://wap.yidianmeii.cn/vnews/0814/87599.shtml
- http://wap.yidianmeii.cn/vnews/0814/3600.shtml
- http://3g.yidianmeii.cn/vnews/0814/191442.shtml

## 项目结构

```
linkvault/
├── bin/                                 # 可执行入口脚本
│   ├── cli.js                           # 命令行主程序，注册 inject/build/start 子命令
│   └── health-check.js                  # 独立运行的探活守护进程，由 crontab 触发
├── config/                              # 全局配置目录
│   ├── default.yaml                     # 默认参数（端口、超时、重试次数、用户代理池）
│   └── schema.json                      # 配置文件的 JSON Schema，用于编辑器智能提示
├── data/                                # 数据存储目录（所有索引和批次清单）
│   ├── batches/                         # 按批次号存放原始链接文件和注入记录
│   │   └── 60_90.txt                    # 第 60/90 批的原始链接列表（每行一个 URL）
│   ├── indices/                         # 元数据索引库
│   │   ├── manifest.json                # 全局批次清单，记录每批的导入时间、链接总数和异常数
│   │   └── url_index.db.json            # 主索引文件（JSON 数组），包含每个 URL 的元数据快照
│   └── logs/                            # 运行日志与异常报告
│       ├── deadlinks.log                # 连续两次探活失败的链接记录
│       └── inject_audit.log             # 每次注入操作的详细审计日志（含校验警告）
├── docs/                                # 文档目录（详见文档导航章节）
├── src/                                 # 核心业务源码
│   ├── core/                            # 引擎核心模块
│   │   ├── injector.js                  # 链接注入与去重逻辑，负责解析原始文件并更新索引
│   │   ├── metadata-fetcher.js          # 异步抓取标题/时间/摘要，含请求队列和限速器
│   │   └── tree-builder.js              # 根据索引生成 ASCII 目录树和导航 HTML
│   ├── utils/                           # 工具函数集合
│   │   ├── url-helper.js                # URL 解析、域名提取、路径规范化
│   │   ├── logger.js                    # 带时间戳的分级日志输出（info/warn/error）
│   │   └── safe-string.js               # Base64 编解码与敏感信息脱敏
│   └── middleware/                      # 用于预览服务的中间件
│       ├── auth-token.js                # 令牌校验，保护解码接口
│       └── rate-limit.js                # 基于 IP 的请求频率限制（默认 100 次/分钟）
├── static/                              # 构建生成的静态资源目录（可托管至 CDN）
│   ├── index.html                       # 总导航页，按批次展示所有链接的摘要表格
│   ├── sitemap.json                     # 结构化站点地图，供搜索引擎或自动化工具消费
│   └── styles/                          # 极简 CSS 样式，适配移动端与桌面浏览
├── test/                                # 单元测试与集成测试用例
│   ├── injector.test.js                 # 覆盖去重、格式容错和边界情况
│   └── fetcher.mock.js                  # 模拟元数据服务，用于离线测试
├── .env.example                         # 环境变量示例（含令牌种子、代理地址、日志级别）
├── package.json                         # 项目声明文件，含所有依赖和 scripts 快捷命令
└── README.md                            # 本文件
```

## 贡献指南

1. 首先在 GitHub 或内部 Git 平台上 Fork 本仓库，并在本地克隆您的 Fork 副本。创建新的功能分支，分支名称需符合 `feature/功能简述` 或 `fix/问题编号` 的格式，例如 `feature/add-csv-export`。

2. 进行代码修改前，请确保已运行 `npm run test` 通过所有现有用例。新增功能需在 `test/` 目录下补充对应的单元测试，且测试覆盖率不得低于 80%。所有源码文件需遵循项目内预设的 ESLint 配置（基于 `eslint:recommended` 与 `plugin:node/recommended`）。

3. 若涉及配置结构或注入格式的变更，必须同步更新 `/docs/` 下对应的文档文件，并在 `README.md` 的「文档导航」表格中增加新文档的索引条目。对于新增的依赖项，需在「安装要求」表格中补充该依赖的名称、版本与引入理由。

4. 提交代码时，使用语义化的提交信息格式：`<类型>(<范围>): <描述>`，其中类型包括 `feat`、`fix`、`docs`、`chore`。提交前会自动触发 pre-commit hook 执行代码格式化（Prettier）和基础语法检查。

5. 最终通过 Pull Request（或内部 Merge Request）将分支合并至主分支。请求描述中需说明本次变更解决的问题、涉及的批次编号（若有）以及测试验证结果。合并后，CI 流水线会自动重新构建静态页面并更新 `data/indices/` 下的索引快照。

## 常见问题

**问：注入时提示“链接格式非法”，但我的 URL 明明是标准 HTTP 地址，可能是什么原因？**

答：系统对每个原始链接会执行三层校验：首先检查是否包含协议头（`http://` 或 `https://`），然后校验域名是否包含至少一个点（`.`），最后检测路径中是否含有可疑字符（如空格、尖括号或非 ASCII 中文）。常见的失败原因是链接末尾意外包含了换行符或制表符，建议使用 `dos2unix` 或 VS Code 的“显示不可见字符”功能清理文件。若确认链接无误，可将该行用双引号包裹后重试。

**问：如何迁移现有的索引数据到另一台服务器？**

答：只需将整个 `data/` 目录打包（不包含 `logs/` 下的临时日志）传输至新服务器的项目根目录下。请确保新服务器的 Node.js 版本与安装要求一致。启动前，建议先执行 `npm run validate` 检查索引文件的完整性，该命令会校验所有 JSON 字段是否缺失必需属性（如 `title`、`fetchedAt`）。若索引文件损坏，可使用 `npm run rebuild -- --from=./backup/manifest.json` 从备份清单尝试恢复。

**问：预览页面展示的链接是经过编码的，如何快速查看原始地址用于调试？**

答：在预览服务启动后，访问 `http://localhost:3000/list?batch=60/90&decoded=true` 并附加查询参数 `token=你的管理令牌`（该令牌在 `.env` 文件中配置，默认为 `VAULT_ADMIN_KEY` 环境变量的值）。注意，该接口会返回完整明文列表，仅限于运维网段内访问，切勿在生产环境暴露此参数。临时查看单个链接时，可点击条目右侧的“解码”按钮，该操作会触发一次短暂的明文弹窗。

## 许可证

MIT

> 外链数量: 10 | 生成时间: 2026-08-14 21:24:15
